import React from "react";

export default class CityLocation extends React.Component {
    render() {
        console.log(this.props)
        return (
            <div>
                <h1>City Location</h1>

            </div>

        );
    }
}